const genHTML = require("./generateHTML")
const questions = [
  
];
var data = {
    color: "green"
}

function writeToFile(fileName, data) {
 
}

function init() {
    console.log(genHTML(data))
}

init();
